# OIDC + Cell 集成 MVP

2026-09-20：按用户确认的[项目宪法](../CONSTITUTION.md)收缩当前里程碑。当前固定版本的核心集成回归已通过，公开集成制品交付尚待完成。企业自托管是方向，不代表当前生产可用。

## 当前目标与分工

两个用户通过 multi-tenant 的 OIDC 登录、创建自己的 Cell、使用原生 DSH，跨用户访问被拒绝。上层负责用户协议、身份/成员、授权和会话；本仓库负责 Cell、资源生命周期与受限访问通道；DSH 负责应用协议、Session 和工具。

集群、namespace、CNI、存储、DNS/TLS 和服务身份权限由管理员配置。平台通过中立内部接口消费 Cell，不建立第二套 Pod/PVC 控制器。正式多后端兼容等待第二个真实需求。

## 最小部署与边界

- 一个集群、Linux 工作节点、上层单副本，一个 OIDC 提供方和固定模板。先验证一套明确配置，可使用 kind。
- 复用现有 Kustomize/测试部署与 Gateway 能力；不以 Helm、两个不同集群的完整安装验证或任意基础设施兼容矩阵阻塞首条链路。
- 固定当次源码/DSH/image 版本。API、配置和状态格式允许随时破坏性变更，不承诺历史兼容、升级、迁移或无感恢复。
- 配置、权限、模板、版本不符合时 fast fail；异步就绪有界等待；错误含阶段、目标、观测状态、写入结果、重试/检查建议及日志关联 ID，且不含秘密。
- 超时不代表取消，记录缺失不代表停止完成。用原标识查询未知结果，不换 key 自动重建，不建设永久终态、无限重试或自动修复系统。
- 不做 HA、多集群、自动扩缩容、备份灾备、通用 runtime 服务、Process/Docker 新后端。当前发行只补管理员已配置 K8s 上的平台 npm 入口，不扩展集群安装产品。已有能力不是新增兼容义务，仍相关的隔离/ownership 检查要保留。

## 当前闭环验收

记录两个仓库提交、镜像 digest、精确 DSH、CNI/存储/CPU 架构、命令与脱敏结果；只针对当前组合。

1. 两个 OIDC 身份与各自 Cell：创建、读取、原生 HTTP/WS/stream/Fetch 可用；未认证、跨用户及绕过入口被拒绝。
2. 父平台会话失效后派生环境会话同时失效，旧连接关闭、新连接拒绝；登出/上层重启不删除 Cell，不承诺停止 DSH 后台任务。
3. 重复创建在当前资源存在期间不重复分配；未知写入、版本不匹配、权限错误提供可由 AI 解读的诊断；旧 UID 删除不影响新实例。
4. 正常 Pod 重建后当前版本的文件/会话仍保留。此项不是历史数据升级、灾难恢复或节点分区 fencing 证明。
5. 一次真实模型调用、唯一文件写入/读回；凭据私下配置。确定性模型可回归但不代替该证据。
6. 管理员删除/测试环境重建说明写清 data、private-state 和外部 Secret 的实际处置范围。只操作明确授权的资源，不静默清空旧数据。

未证实清理完成时拒绝复用旧数据并交给管理员，不把控制对象消失等同物理 writer 停止。不额外扩展为通用恢复/迁移机制。

## 历史能力与发行

已发布产物和原独立 OIDC/RBAC 路径描述它们自己的版本，不是新版本兼容承诺。本次只改变开发原则和计划，没有发布新产物或自动迁移现有环境。原 chart/卸载重装/多环境安装目标如有真实需要另行安排，不作为当前集成 MVP 的隐含门槛。

实际安装或发布变更仍需验证当次产物身份与流程；自动 CI 仍仅 Source standards。发布、部署和删除数据按用户已有授权处理。

[English](alpha-mvp.md)

实际结果和未覆盖项见 [2026-09-20 集成回归](https://github.com/GuoMonth/dsh-multi-tenant/blob/4ba252765bccb41314c0bdc6b11bcf60cc0b33ef/docs/evidence/cell-regression-2026-09-20.md)。当前交付为管理员预配置 K8s 上的平台 npm CLI，不增加集群安装器；历史 R1–R6 待测清单不代表现在仍未测试。
