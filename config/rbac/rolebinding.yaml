apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: dsh-isolated-control-plane
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: dsh-isolated-control-plane
subjects:
  - kind: ServiceAccount
    name: dsh-isolated-control-plane
    namespace: dsh-isolated-system
