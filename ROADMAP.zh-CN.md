[English](./ROADMAP.md) | 简体中文

# 路线图

状态：✅ 已完成 · 🚧 下一步（已确定） · ⏳ 延后（待决策）。

初始里程碑是 **M0 —— 启动**：搭建仓库，并为组成隔离运行时控制平面的四个组件划定范围——
先放在一个地方，而非过早拆分。

## 下一步（已确定）

- 🚧 **M1 —— Scheduler。** 在资源与安全策略约束下，将每个租户会话调度到独享的运行时
  （Pod）；将调度决策暴露为稳定接口，以便未来由非 Kubernetes 后端实现。
- 🚧 **M2 —— Gateway。** 将会话路由到其隔离运行时的准入点；"哪个租户、哪个运行时"
  的唯一决策处。
- 🚧 **M3 —— checkpoint/restore。** 跨 Pod 重调度捕获并恢复会话的运行时状态，使隔离不
  以牺牲会话连续性为代价。
- 🚧 **M4 —— runtime images。** 按工作负载（Terminal、Python、Node、……）提供标准运行时
  镜像与 profile，并附版本化的 profile 契约。

## 延后（待决策）

- ⏳ **运行时无关的后端。** 先在第二个容器运行时接口（Docker / Firecracker / Nomad）上
  验证控制平面，再行泛化。
- ⏳ **公共契约冻结。** 在 M1–M4 确定组件边界之前，命名与 surface 均为暂定。

## 里程碑

- **M0 —— 启动** 🚧 仓库、双语文档、初始范围划定。
- **M1 —— Scheduler。**
- **M2 —— Gateway。**
- **M3 —— checkpoint/restore。**
- **M4 —— runtime images。**
- **M5 —— 端到端隔离套件** —— 可执行的证明：租户无法逃逸其运行时边界（网络、文件系统、
  宿主机）。

每个里程碑都以前一个里程碑的决策为前提；延后项只有在其门槛（一项决策或一个上游 seam）
关闭时才会被提前。
